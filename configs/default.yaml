# DataPilot default configuration.
# Values here are placeholders; nothing consumes them yet beyond
# datapilot.config.load_config().

project:
  name: DataPilot
  # Highest fully-implemented data-engine phase. Phases 0-7 are done
  # (Phase 7 complete + a post-Phase-7 stabilization pass); Phase 8
  # (deep learning) and later are not started. See docs/roadmap.md.
  phase: 7

paths:
  data_raw: data/raw
  data_processed: data/processed
  data_interim: data/interim
  artifacts: artifacts

# LLM provider abstraction — no provider is wired yet.
ai_engine:
  provider: null          # e.g. "anthropic" | "openai" | "local"
  model: null
  temperature: 0.0

determinism:
  global_seed: 42
