"""DataPilot shared core package.

Holds cross-cutting concerns used by every engine: version metadata,
configuration loading, and (in later phases) shared data contracts.
It contains no data-science logic itself.
"""

__version__ = "0.0.0"
